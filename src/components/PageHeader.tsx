// Re-export from canonical location for backwards-compatibility
export { PageHeader, PageHeaderPortalContext } from '@/components/ui/PageHeader';
export type { PageHeaderProps } from '@/components/ui/PageHeader';
